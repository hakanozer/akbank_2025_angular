import { PageTitle } from './page-title';

describe('PageTitle', () => {
  it('should create an instance', () => {
    const directive = new PageTitle();
    expect(directive).toBeTruthy();
  });
});
